/**
 * @raisedao/api — Express API server and the in-process chain indexer.
 * The server skeleton lands in issue #12; this placeholder keeps the package buildable.
 */
export const PACKAGE_NAME = '@raisedao/api';
