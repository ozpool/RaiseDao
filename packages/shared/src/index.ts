/**
 * @raisedao/shared — cross-package types, env validation, contract addresses and ABIs.
 * Populated in issue #4; this placeholder keeps the package buildable from milestone 1.
 */
export const PACKAGE_NAME = '@raisedao/shared';
